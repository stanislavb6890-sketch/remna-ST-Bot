# CLOAKNET 3.0 — Backend (для локальной разработки)
# В Docker все переменные приходят из корневого .env через docker-compose.yml

NODE_ENV=development
PORT=5001

# PostgreSQL
DATABASE_URL=postgresql://CLOAKNET:password@localhost:5432/CLOAKNET

# JWT (минимум 32 символа)
JWT_SECRET=your_jwt_secret_min_32_chars_change_in_production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Первый админ
INIT_ADMIN_EMAIL=admin@CLOAKNET.local
INIT_ADMIN_PASSWORD=

# Remnawave API
REMNA_API_URL=https://panel.example.com
REMNA_ADMIN_TOKEN=

# CORS
CORS_ORIGIN=http://localhost:5173
