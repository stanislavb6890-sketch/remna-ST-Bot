###############################################################################
#  CLOAKNET 3.0 — Docker Compose
#  Репозиторий: https://github.com/CLOAKNET-APP/remnawave-CLOAKNET-Bot
#  Запуск: docker compose up -d
###############################################################################
name: CLOAKNET

services:
  # ── PostgreSQL ──────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: CLOAKNET-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-CLOAKNET}
      POSTGRES_USER: ${POSTGRES_USER:-CLOAKNET}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-CLOAKNET_change_me}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - CLOAKNET
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-CLOAKNET} -d ${POSTGRES_DB:-CLOAKNET}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── Backend API ─────────────────────────────────────────────────
  # Порт 5000 на хосте нужен только когда nginx снаружи (USE_BUILTIN_NGINX=false):
  # хост-nginx проксирует на 127.0.0.1:5000. Бинд только на localhost — чтобы
  # API не был доступен с внешних интерфейсов.
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: CLOAKNET-api
    restart: unless-stopped
    ports:
      - "0.0.0.0:5000:5000"
    environment:
      NODE_ENV: production
      PORT: "5000"
      DATABASE_URL: "postgresql://${POSTGRES_USER:-CLOAKNET}:${POSTGRES_PASSWORD:-CLOAKNET_change_me}@postgres:5432/${POSTGRES_DB:-CLOAKNET}"
      JWT_SECRET: "${JWT_SECRET}"
      JWT_ACCESS_EXPIRES_IN: "${JWT_ACCESS_EXPIRES_IN:-15m}"
      JWT_REFRESH_EXPIRES_IN: "${JWT_REFRESH_EXPIRES_IN:-7d}"
      CORS_ORIGIN: "https://${DOMAIN:-localhost}"
      REMNA_API_URL: "${REMNA_API_URL:-}"
      REMNA_ADMIN_TOKEN: "${REMNA_ADMIN_TOKEN:-}"
      INIT_ADMIN_EMAIL: "${INIT_ADMIN_EMAIL:-admin@CLOAKNET.local}"
      INIT_ADMIN_PASSWORD: "${INIT_ADMIN_PASSWORD:-}"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - CLOAKNET

  # ── Telegram Bot ────────────────────────────────────────────────
  bot:
    build:
      context: ./bot
      dockerfile: Dockerfile
    container_name: CLOAKNET-bot
    restart: unless-stopped
    environment:
      BOT_TOKEN: "${BOT_TOKEN:-}"
      API_URL: "http://api:5000"
    depends_on:
      - api
    networks:
      - CLOAKNET

  # ── Frontend Build ──────────────────────────────────────────────
  # Одноразовый контейнер для сборки фронтенда в volume
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: CLOAKNET-frontend-build
    volumes:
      - frontend_dist:/out
    entrypoint: ["sh", "-c", "rm -rf /out/* && cp -r /dist/* /out/ && echo 'Frontend copied to volume'"]

  # ── Nginx (встроенный) ─────────────────────────────────────────
  # Включается когда USE_BUILTIN_NGINX=true
  nginx:
    image: nginx:1.28-alpine
    container_name: CLOAKNET-nginx
    restart: unless-stopped
    profiles:
      - builtin-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - frontend_dist:/var/www/CLOAKNET:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - certbot_www:/var/www/certbot:ro
      - certbot_certs:/etc/nginx/ssl:ro
    depends_on:
      frontend:
        condition: service_completed_successfully
      api:
        condition: service_started
    networks:
      - CLOAKNET

  # ── Certbot (авто-SSL) ─────────────────────────────────────────
  certbot:
    image: certbot/certbot:latest
    container_name: CLOAKNET-certbot
    profiles:
      - builtin-nginx
    volumes:
      - certbot_www:/var/www/certbot
      - certbot_certs:/etc/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - CLOAKNET

volumes:
  postgres_data:
  frontend_dist:
  certbot_www:
  certbot_certs:

networks:
  CLOAKNET:
    driver: bridge
    enable_ipv6: true
